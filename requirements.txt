# Reproducibility dependencies. Lower bounds are what the code requires;
# the pipeline was last verified with the versions noted in parentheses.
numpy>=1.21        # (tested: 2.4.4)
scipy>=1.7         # (tested: 1.17.1)
pandas>=1.3        # (tested: 3.0.2)
matplotlib>=3.5    # (tested: 3.10.8)
# Note: PyTorch is NOT required to reproduce any table/number/figure in the paper.
# It is only needed for the optional Figure-7 DFT panel (future-work control),
# which is not part of the released results.
